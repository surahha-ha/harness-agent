# v0 완료 기준 — 도달선

> **성격: 규범 문서.** 여기 적힌 기준은 구현이 못 미친다고 조용히 낮추지 않는다.
> 낮출 때는 이 문서에 근거를 적고 바꾼다. (서술 문서라면 코드가 이기지만, 도달선은 반대다.)

## 왜 이 문서가 있나

완료 기준이 없으면 골격을 다듬는 일이 끝나지 않는다 — "더 좋게" 는 종료 조건이 아니다.
그리고 이 기준은 뒤 단계(eval 하네스)의 **첫 측정 항목**을 겸한다. v0 이 끝났는지 재는 방법이
곧 하네스가 동작하는지 재는 방법이다.

## 기준 다섯 — 전부 측정 가능해야 한다

| # | 기준 | 측정 방법 | 현재 |
| - | - | - | - |
| 1 | **부트스트랩 30분** — 문서만 보고 ①복사→④활성까지 | 필드 리포트에 소요 시간을 적는다 | 🟢 **7분** — 2회차에서 계측(필드 리포트 2 §1). 훅 연결·발동 확인까지 포함한 시간 |
| 2 | **자동 발동 ≥1** — 골격 1 이 검증 사다리 ③(대화형 발동)까지 확인된 프로젝트가 하나 이상 | 규칙에 걸리는 무해 프로브가 실세션에서 deny 판정을 받는다 | 🟢 1 — 대화형 실세션에서 deny 발동·서명 확인 (필드 리포트 1 §5, F11·F12) |
| 3 | **실행 골격 3종이 돈다** — 골격 1(위험 명령 가드) · 2(test-first `--audit`) · 4(드리프트 감시)는 코드로 구현되고 단위 테스트가 있다 | 각 골격의 테스트 그린 + `--status` 류 상태 보고 | 🟢 3종 구현 · 테스트 67(가드 26 · test-first 27 · drift 14) · 각각 `--status` 보고 |
| 4 | **실적용 2회, 그중 1회는 짝 저장소** — 골격 4 와 계약 커플링이 발동할 조건이 실재하는 곳 | 필드 리포트 2 존재 + 골격 4 발동 기록 | 🟢 **2회 · 짝 1회** — 골격 4 는 검출→승인→침묵 전 경로 실증(필드 리포트 2 F18) |
| 5 | **반출 게이트 전 커밋 그린** | 커밋마다 `scan-forbidden` 통과 · 대상 프로젝트 식별명 0건 | 🟢 유지 중 |

## v0 에 넣지 않는 것 (경계)

- **골격 3(역할 경계) · 5(문서 규약) · 6(품질 사이클) · 7(배포 기계)는 절차 문서까지가 v0.**
  코드화는 v1 이후다. 근거: 이 넷은 발동 조건이 프로젝트 상황(역할 분화·짝 채널)에 있어,
  실물 없이 코드부터 쓰면 패널이 경고한 "만드는 사람의 시선" 으로 돌아간다.
- **패널 조건부 2건**(인계 기계검증 · 채널 1개 가정)은 v0 완료를 막지 않는다 —
  실물이 조건을 만들어야 닫히는 항목이라고 이미 판정했다.
- **eval·자동 최적화는 v0 밖.** 이 문서의 기준 1·2 가 그때의 첫 metric 이 된다.
  최종 목표(완전 자동화)까지의 단계 전체는 `08-roadmap.md` 에 있다.

## 판정 — v0 도달 (2026-08-14)

**다섯 기준 전부 그린.** 2회차 적용에서 기준 1(7분)과 4(짝 저장소)가 닫혔고,
그 과정에서 골격 2 의 `--audit` 선실측과 골격 4 의 검출→승인→침묵이 처음으로 실제 프로젝트에서 돌았다.

⚠️ **기준이 말하지 않은 것을 달성했다고 읽지 않는다.** 이 문서의 기준은 측정 가능한 것만 골랐으므로,
아래는 v0 도달과 무관하게 **아직 안 쟀다** — v1 의 입력이다.

- **계약 커플링을 재지 못했다.** 짝 저장소를 걸었지만 감시 대상은 에이전트 설정 자산이고,
  한쪽만 고치면 런타임에 깨지는 **코드 계약**은 아직 어느 골격도 보지 않는다.
- **하네스 사본이 낡는 경로**(골격 7 의 조건)가 이번에 처음 생겼을 뿐 아직 발동하지 않았다.
- **역할이 둘 이상인 프로젝트**를 아직 못 만나 패널 조건부 3-전문가는 여전히 열려 있다.
